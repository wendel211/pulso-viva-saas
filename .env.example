# Banco transacional (Postgres). Para o docker-compose local:
DATABASE_URL="postgresql://pulsoviva:pulsoviva@localhost:5432/pulsoviva"

# Segredo para assinar a sessão (JWT). Gere com: openssl rand -base64 32
SESSION_SECRET="troque-este-valor-em-producao"
